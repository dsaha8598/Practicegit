import { ToprecisionPipe } from './toprecision.pipe';

describe('ToprecisionPipe', () => {
  it('create an instance', () => {
    const pipe = new ToprecisionPipe();
    expect(pipe).toBeTruthy();
  });
});
