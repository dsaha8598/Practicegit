import { NumberlengthvalidatorDirective } from './numberlengthvalidator.directive';

describe('NumberlengthvalidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberlengthvalidatorDirective();
    expect(directive).toBeTruthy();
  });
});
