package com.ey.in.tds.onboarding.service.ldc;

import org.springframework.stereotype.Service;

@Service
public class LdcMasterServiceTest {

	/*
	 * @Autowired LdcMasterRepository ldcMasterRepository;
	 */

	//TODO NEED TO CHANGE FOR SQL
	
	
/*	@Test
	public void create(LdcMaster ldcMaster) {
		ldcMaster = ldcMasterRepository.insert(ldcMaster);
		assertTrue(ldcMaster != null);
	}

	@Test
	public void get(LdcMaster.Key key) {
		ldcMasterRepository.findById(key).get();
		assertTrue(true);
	}

	@Test
	public void getAll(List<LdcMaster.Key> keys) {
		ldcMasterRepository.findAllById(keys, "key");
		assertTrue(true);
	}

	@Test
	public void getLdcMastersByAssessmentYearPanDeductorTan(int assessmentYear, String pan, String deductorTan) {
		ldcMasterRepository
				.getLdcMastersByAssessmentYearPanDeductorTan(assessmentYear, pan, deductorTan, Pagination.DEFAULT)
				.getData();
		assertTrue(true);
	}   */
}
