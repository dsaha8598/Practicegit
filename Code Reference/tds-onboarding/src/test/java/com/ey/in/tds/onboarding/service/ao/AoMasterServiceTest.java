package com.ey.in.tds.onboarding.service.ao;

import org.springframework.stereotype.Service;

@Service
public class AoMasterServiceTest {

/*	@Autowired
	AoMasterRepository aoMasterRepository;

	@Test
	public void create(AoMaster aoMaster) {
		aoMasterRepository.insert(aoMaster);
		assertTrue(true);
	}

	@Test
	public void get(AoMaster.Key key) {
		aoMasterRepository.findById(key).get();
		assertTrue(true);
	}

	@Test
	public void getAll(List<AoMaster.Key> keys) {
		aoMasterRepository.findAllById(keys, "key");
		assertTrue(true);
	}

	@Test
	public void getAoMasterByAssessmentYear(int assessmentYear, int assementMonth, String deductorTan) {
		aoMasterRepository.getAoMastersByAssessmentYearAssessmentMonthDeductorMasterTan(assessmentYear, assementMonth,
				Arrays.asList(deductorTan), Pagination.DEFAULT).getData();
		assertTrue(true);
	}  */

}
