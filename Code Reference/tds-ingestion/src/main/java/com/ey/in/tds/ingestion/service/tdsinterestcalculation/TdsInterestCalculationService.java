package com.ey.in.tds.ingestion.service.tdsinterestcalculation;

import org.springframework.stereotype.Service;

@Service
public class TdsInterestCalculationService {

/*	public TdsInterestCalculation create(TdsInterestCalculation TdsInterestCalculation) {
		TdsInterestCalculation.getKey().setId(UUID.randomUUID());
		return tdsInterestCalculationRepository.insert(TdsInterestCalculation);
	}   */

/*	public TdsInterestCalculation get(TdsInterestCalculation.Key key) {
		Optional<TdsInterestCalculation> response = tdsInterestCalculationRepository.findById(key);
		if (response.isPresent()) {
			return response.get();
		}
		return null;
	}  */

/*	public PagedData<TdsInterestCalculation> getTdsInterestCalculationByAssessmentYear(int assessmentYear,
			int assessmentMonth, Pagination pagination) {
		return tdsInterestCalculationRepository
				.getTdsInterestCalculationsByAssessmentYearAssessmentMonth(assessmentYear, assessmentMonth, pagination);
	}  */
}