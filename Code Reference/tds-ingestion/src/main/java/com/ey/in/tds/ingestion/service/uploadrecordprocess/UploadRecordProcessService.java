package com.ey.in.tds.ingestion.service.uploadrecordprocess;

import org.springframework.stereotype.Service;

@Service
public class UploadRecordProcessService {
/*

	public UploadRecordProcess create(UploadRecordProcess uploadRecordProcess) {
		uploadRecordProcess.getKey().setId(UUID.randomUUID());
		return uploadRecordProcessRepository.insert(uploadRecordProcess);
	}

	public UploadRecordProcess get(UploadRecordProcess.Key key) {
		Optional<UploadRecordProcess> response = uploadRecordProcessRepository.findById(key);
		if (response.isPresent()) {
			return response.get();
		}
		return null;
	}

	public PagedData<UploadRecordProcess> getUploadRecordProcessByAssessmentYear(int assessmentYear,
			Pagination pagination) {
		return uploadRecordProcessRepository.getUploadRecordProcessByAssessmentYear(assessmentYear, pagination);
	}   */

}
