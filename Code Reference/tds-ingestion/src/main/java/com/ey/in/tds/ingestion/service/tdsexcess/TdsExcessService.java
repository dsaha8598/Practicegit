package com.ey.in.tds.ingestion.service.tdsexcess;

import org.springframework.stereotype.Service;

@Service
public class TdsExcessService {

/*	public TdsExcess create(TdsExcess TdsExcess) {
		TdsExcess.getKey().setId(UUID.randomUUID());
		return tdsExcessRepository.insert(TdsExcess);
	} */

/*	public TdsExcess get(TdsExcess.Key key) {
		Optional<TdsExcess> response = tdsExcessRepository.findById(key);
		if (response.isPresent()) {
			return response.get();
		}
		return null;
	}  */

/*	public PagedData<TdsExcess> getTdsExcesssByAssessmentYearAssessmentMonthDeductorTan(int assessmentYear,
			int assessmentMonth, List<String> deductorTan, Pagination pagination) {
		return tdsExcessRepository.getTdsExcesssByAssessmentYearAssessmentMonthDeductorTan(assessmentYear,
				assessmentMonth, deductorTan, pagination);
	}  */

}